variables:
  
  # Azure Resource Manager connection created during pipeline creation
  connectedServiceNameNonPROD: 'Crozy Subscription Non-PROD'  

  # Agent VM image name
  vmImageName: 'windows-latest'

  # Working Directory
  workingDirectory: '$(System.DefaultWorkingDirectory)/Crozy.GraphQLApi'

  buildConfiguration: 'Release'