

export const useUploadFile = () => {

  // const uploadFile = async (file: File): Promise<StoragePutOutput<any>> =>
  //   await Storage.put(file.name, file);

  // const getImage = async (url: string) => await Storage.get(url);

  // return { uploadFile, getImage };
};
